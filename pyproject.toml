[project]
name = "image-caption-generator"
version = "0.1.0"
description = "Minimal image caption training pipeline"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "clearml>=2.0",
    "nltk>=3.9",
    "numpy>=2.2",
    "pandas>=2.3",
    "pillow>=12.1",
    "pyyaml>=6.0",
    "torch>=2.3",
    "torchvision>=0.18",
    "tqdm>=4.67",
]
